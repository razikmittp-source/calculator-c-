// CoreC — Fibonacci sequence
fn fibonacci(n: i32) -> i32 {
    if n <= 1 {
        return n
    }
    return fibonacci(n - 1) + fibonacci(n - 2)
}

fn main() {
    print("Fibonacci sequence:")
    for i in [0..20] {
        let result = fibonacci(i)
        print("  fib({i}) = {result}")
    }
}
