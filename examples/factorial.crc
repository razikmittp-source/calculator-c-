// CoreC — Factorial with recursion
fn factorial(n: i32) -> i32 {
    if n <= 1 {
        return 1
    }
    return n * factorial(n - 1)
}

fn main() {
    for i in [1..13] {
        let f = factorial(i)
        print("{i}! = {f}")
    }
}
